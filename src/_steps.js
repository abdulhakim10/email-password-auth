/*
'register' or 'sign up' [First time create a user] 
'log in' === 'sign in' [Already you have a user account created]

*/

/*
    get form value:
    set form onSubmitHandler
    set event parameter
    event.target.name.value
*/